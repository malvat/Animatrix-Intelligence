services:
  animatrixz-intelligence:
    build: .
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    env_file:
      - backend/.env
    environment:
      NODE_ENV: production
      PORT: ${PORT:-5000}
      CLIENT_URL: ${CLIENT_URL:-http://localhost:5000}
    restart: unless-stopped
